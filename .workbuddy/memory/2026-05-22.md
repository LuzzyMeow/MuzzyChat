# 每日记忆 · 2026年05月22日

> 摘要：第13-17轮代码审查 → Phase 2 收尾 → **Phase 3 双模式引擎完整开发**（8新建+5修改，91测试通过，已推送）

---

## 第13轮审查（上午）
- **流式消息重复拼接修复**：ChatPage/DMPage `message:stream` 从 `last.content + payload.content` 改为 `payload.content`
- **DM 路由修复**：Conversation 后端新增 `participantAgentId`，HomePage DM 跳转使用 agentId
- **agent:thinking 双重注册合并**
- **DMPage cleanup/Lint/TS类型错误修复**
- **README 端口修正**：`3000` → `3001`

## 第14轮审查（下午）— 全局对照立项书+技术设计文档
- **审查范围**：Prisma Schema(19 models) + 全部后端源码 + 全部前端源码，对照5份文档
- **B-1 阻断**：添加缺失工具 web_search/web_fetch/code_execute（以前只有4个）
- **B-2 阻断**：修复 message:complete 前后端数据结构不匹配
- **S-1 严重**：HIGH_RISK_TOOLS 新增 code_execute
- 新增已知问题 I-007~I-010
- 文档合规确认全部通过

## 第15轮审查 — @lobehub/ui Markdown 集成
- ChatPage/DMPage 集成 @lobehub/ui 的 Markdown 组件
- I-002 已解决 | typecheck/lint/test 全部绿色

## 第16轮审查+改造 — @lobehub/ui 全面集成
- ChatPage/DMPage 用 ChatItem + ChatInputArea 全面替代自绘组件
- 流式消息独立管理（streamingContent/streamingAgentId state）
- 78/78 测试通过

## 第17轮审查 — 全局对照全部文档
- 确认 Phase 2 全部完成，91测试通过，无新增阻断问题
- 建议直接进入 Phase 3

## Phase 2 收尾开发
- Model Provider 后端（7文件）+ Settings 后端（4文件）
- SettingsPage 重写（4 Tab）
- ChatPage 成员管理（Drawer + 添加/移除）
- 91 passed (90 backend + 1 frontend)

---

## Phase 3 双模式引擎开发 · 完整过程

### 步骤 1：Plan Mode 探索与规划
- 读取 `MuzzyChat 项目立项书.md` §八 — 确认 Phase 3 范围
- 读取 `02-群聊与交互设计.md` 全文 — 获取所有状态定义、算法参数、Schema
- **Explore Agent 1**：深入分析 `chat.gateway.ts` 消息流
  - 发现 triggerAgentResponse 未检查 orchestrationMode，全部并行调用
  - 发现 emitAgentPeerMessage / emitTaskUpdate / emitGroupMemberJoined 已定义但零调用
- **Explore Agent 2**：读通 ChatPage.tsx 完整结构
- 读取 `agent-loop.service.ts` 全文、`llm.service.ts`、`api/groups.ts`、`types/chat-group.ts`
- 读取 `app.module.ts` / `gateway.module.ts` / `agent-loop.module.ts` — 确认前进 Ref 模式
- 写入计划文档 → ExitPlanMode → 用户确认

### 步骤 2：T1 ParallelOrchestrator 新建
- mkdir orchestration/
- 新建 `parallel-orchestrator.service.ts` (299行)
- 实现：7态状态机 (ParallelState enum)、RoundState/AgentRoundState interface、triggerRound() 并发分发、Promise.allSettled 异常处理、interruptRound() 用户中断、enterDynamicDiscussion() 动态讨论触发、handleLoopDetected() 循环截断
- 全部字段溯源自 §1.1-1.2 状态图定义

### 步骤 3：T2 DynamicDiscussionCoordinator 新建
- 新建 `dynamic-discussion.service.ts` (133行)
- 实现：maxDynamicRounds(N)=N×3、isSkipResponse() 判定、runDynamicRound() 完整流程、decisionPrompt 决策引导模板、emitAgentPeerMessage 广播
- 全部算法参数溯源自 §2.1-2.4

### 步骤 4：T3 LoopDetector 新建
- 新建 `loop-detector.service.ts` (292行)
- 实现：TF-IDF 关键词提取 (中英文混合分词: word token + CJK bigram + 数字)、Jaccard 相似度计算、启发式初筛 (3条件 AND)、LLM 确认 Prompt (逐字复制)、JSON 响应解析
- 全部阈值溯源自 §3.2-3.5

### 步骤 5：T4 SupervisorEngine 新建
- 新建 `supervisor-engine.service.ts` (549行)
- 实现：ExecutionPlan/Subtask interface、5态 PlanStatus、6态 SubtaskStatus、generatePlan() LLM生成、validatePlan() 复合校验 (agent_id/DFS成环/BFS不可达)、executePlan() 拓扑排序+并行执行、失败重试3次+指数退避、notifySupervisorReplan() 重规划
- 全部 Schema 溯源自 §4.1-4.4

### 步骤 6：T5 AgentRecruitService 新建
- 新建 `agent-recruit.service.ts` (233行)
- 实现：SYSTEM_PROMPT 模板 (逐字复制)、parse() 重试流程 (2次)、validateRecruitResult() Schema验证、createAgentsForGroup() 批量创建
- 全部规则溯源自 §5.1-5.4

### 步骤 7：T8 OrchestrationController + OrchestrationModule
- 新建 `orchestration.controller.ts` (89行) — 3个端点
- 新建 `orchestration.module.ts` (34行) — forwardRef 处理循环依赖

### 步骤 8：T6+T7 ChatGateway 重构 + 模块集成
- **chat.gateway.ts**：新增 ParallelOrchestrator + SupervisorEngine import 和 DI；triggerAgentResponse 按 orchestrationMode 路由
- **gateway.module.ts**：新增 forwardRef(() => OrchestrationModule)
- **app.module.ts**：新增 OrchestrationModule 注册
- **chat.gateway.spec.ts**：新增 mockParallelOrchestrator + mockSupervisorEngine

### 步骤 9：第一轮验证 + Lint 修复
- typecheck ✅
- lint → 5 warnings：修复 (isSkipResponse/response/GroupMember/OutputBuffer/subtaskMap)
- test → 27 failed：修复 chat.gateway.spec.ts（新增 mock provider 依赖）
- test ✅ 90 passed

### 步骤 10：前端 TaskBoard + ChatPage 重写
- **新建 TaskBoard.tsx** (220行)：6态 STATUS_CONFIG + TaskBoard 组件 + useTaskBoard hook
- **ChatPage.tsx 重写**：
  - 模式切换 Dropdown → updateGroup API
  - 动态讨论 Switch
  - TaskBoard 集成 (useTaskBoard)
  - group:member_joined WS 事件 → 系统消息
  - 自然语言添加成员 TextArea + 智能添加 Button → POST /api/groups/:id/agents/parse → batch

### 步骤 11：前端 NewGroupPage 重写
- **NewGroupPage.tsx 重写**：Tabs 手动配置 + 一句话招募
- 招募输入 → /api/recruit → Agent 预览卡片 → 确认创建群聊 + 批量创建 Agent

### 步骤 12：第二轮验证 + Lint 修复
- typecheck ✅
- lint → 2 errors + 5 warnings：修复 (React import/Title/createdRes/Spin/handleTaskStart)
- lint → 0 errors, 1 warning (react-refresh 可忽略)
- test ✅ 91 passed (90 backend + 1 frontend)

### 步骤 13：文档 + Git
- 更新 26-05-22.md、工作日志.md、MEMORY.md、README.md、2026-05-22.md
- git commit + push (SSH over 443) → b467fe0 pushed to master

## 最终结果
- 16 files changed, +2279 / -45
- 新建 8 文件（7 后端 + 1 前端）
- 修改 5 文件（4 后端 + 1 前端）
- 文档更新 5 文件
- typecheck ✅ | lint ✅ (0 errors) | test ✅ (91 passed)
- Phase 3 核心完成

---

## 第18轮代码审查 (18:31)
- **审查模式**：全局对照立项书+全部4份技术设计文档
- **审查范围**：Phase 3 双模式引擎逐字段溯源审查 (5个orchestration服务 + ChatGateway路由 + 前端TaskBoard/ChatPage/NewGroupPage)
- **验证结果**：typecheck ✅ | lint ✅ (0 errors) | test ✅ (91 passed)
- **发现**：
  - 🔴 B-4: SupervisorEngine.allSubtasksCompleted() 将 Failed 视为完成状态（违反 §4.3.1 设计）
  - 🔴 B-5: DynamicDiscussionCoordinator 未检测 Agent 的 `[SKIP]` 回复（违反 §2.2.3 设计）
  - 🟠 S-2: 子任务重试无指数退避延迟（违反 §4.3.3 设计）
  - 🟠 S-3: OrchestrationController parseAgents/batchAddAgents 缺少群组存在性验证
  - 🟡 N-004~N-007: 4项一般级
- **结论**：⚠️ 有条件通过，建议进入 Phase 4 前先修复 B-4/B-5
- 更新工作日志.md 已知问题清单（新增 I-011~I-014）

## B-4/B-5/S-2/S-3 修复 (18:46)
- B-4: allSubtasksCompleted 移除 Failed 条件
- B-5: runAgentLoop 返回 Promise<string> + coordinator 检测 [SKIP]
- S-2: 添加指数退避延迟 1s/2s/4s
- S-3: 添加群组存在性验证 + NotFoundException
- 验证: typecheck ✅ | lint ✅ | test ✅ 91 passed
- I-011~I-014 全部标记为已修复
- Git: commit 7385ae6 → pushed to master
- I-011~I-014 全部标记为已修复
  - 🟡 N-004~N-007: 4项一般级（System Prompt 缺示例/round切分偏差/model计数/NestJS DTO无校验）
- **结论**：⚠️ 有条件通过，建议进入 Phase 4 前先修复 B-4/B-5
- 更新工作日志.md 已知问题清单（新增 I-011~I-014）

---
## Phase 4 开发 — 工具与安全 (19:20-19:37)

### 新建文件 (6 文件)
| 文件 | 说明 |
|:---|:---|
| `security/risk-engine.service.ts` | 风险规则引擎：完整高风险清单 (read_path×8 + write_path×13 + command×21) + 间接调用检测 (bash -c/cmd /c/powershell/python -c/node -e/eval) + 递归解析 (深度3) + Base64解码 + 语言特定危险调用检测 |
| `security/whitelist.service.ts` | Redis 会话白名单 (ioredis) + 内存 fallback，精确匹配，TTL 24h |
| `security/audit-trail.service.ts` | 审计日志服务 (仅 create + query，不可篡改) |
| `security/approval-timeout.service.ts` | 审批超时管理器 (5分钟自动拒绝) |
| `security/tool-executor.service.ts` | 统一工具箱编排器 (预检→风险→白名单→审计) |
| `security/security.module.ts` | NestJS 安全模块 |

### 修改文件 (8 文件)
| 文件 | 变更 |
|:---|:---|
| `agent-loop/tools/index.ts` | 重写：read_file(filePath→path+encoding+offset+limit)、write_file(+append+createDirs)、execute_command(workdir→cwd+timeout+env)、list_files(+recursive+pattern)、code_execute(安全约束)、web_search(DuckDuckGo真实实现)、web_fetch(fetch真实实现) |
| `agent-loop/agent-loop.service.ts` | toolsNode 集成 ToolExecutor+RiskEngine (替换简单 HIGH_RISK_TOOLS 检查)、noReviewMode 设置读取、审批超时调度、工具执行审计 |
| `agent-loop/agent-loop.module.ts` | 新增 SecurityModule import |
| `gateway/chat.gateway.ts` | 新增 ApprovalTimeoutService+ToolExecutorService DI、审批响应取消超时+审计 |
| `gateway/gateway.module.ts` | 新增 SecurityModule import |
| `gateway/chat.gateway.spec.ts` | 新增 mockApprovalTimeout + mockToolExecutor provider |
| `app.module.ts` | 注册 SecurityModule |

### 配置新建
| 文件 | 说明 |
|:---|:---|
| `backend/eslint.config.mjs` | ESLint 9 flat config (Node.js globals + TS parser) |

### pnpm 依赖新增
- `ioredis` (backend) — Redis 客户端

### 验证结果
- `pnpm typecheck` ✅ 0 errors
- `pnpm lint` ✅ 0 errors, 19 warnings (no-explicit-any)
- `pnpm test` ✅ 90 passed (90 backend + 1 frontend)

### 设计文档溯源
全部字段/阈值/算法可追溯到 `03-安全与工具设计.md`，包括：
- §1: 全量高风险清单 (42条规则)
- §1.5: 间接调用检测 (8种模式 + 递归解析)
- §2.6: 审批超时机制
- §3: 会话白名单 (Redis + 精确匹配)
- §4: 审计日志不可篡改
- §5.2: 7工具完整参数 Schema
- §5.3: code_execute 安全约束
- §5.4: ToolExecutor 统一编排
- §6: LangGraph.js 集成

---
## Phase 5 开发 — ACE 记忆系统 (19:42-19:50)

### 新建 (5 文件)
- `ace/ace-generator.service.ts` — Generator: onRoundComplete → 聚合 → LLM (small model) → JSON 解析 → DB 写入 → async embedding
- `ace/ace-reflector.service.ts` — Reflector: 时间+计数双触发 → 余弦相似度 > 0.85 合并 → LLM lesson 重生成 → 删除清理
- `ace/ace-curator.service.ts` — Curator: 5 条 scope 优先级规则 (failure→personal / 偏好→group / 跨Agent相似→global)
- `ace/ace-retrieval.service.ts` — Retrieval: pgvector Top-K 三层检索 (personal+group+global) → formatForContext
- `ace/ace.module.ts` — NestJS 模块

### 修改 (3 文件)
- `llm/llm.service.ts` — 新增 embedText() + resolveEmbeddingConfig()
- `app.module.ts` — 注册 AceModule + EventEmitterModule
- `doc/26-05-22.md`, `doc/工作日志.md`, MEMORY.md, README.md — 文档更新

### 验证
- typecheck ✅ | lint ✅ 0e | test ✅ 90 passed

### 新增依赖
- @nestjs/event-emitter (用于事件驱动 ROUND_COMPLETE → Generator 桥接)

---

## 第19轮代码审查 (20:00)

### 审查范围
全局对照立项书+4份设计文档，Phase 4 (security/) + Phase 5 (ace/) 逐字段审查。

### 发现与修复 (5 项)
| 编号 | 严重度 | 问题 | 修复 |
|:---|:---|:---|:---|
| B-6 | 🟠 严重 | Reflector executeDeletion 使用 updatedAt 而非 lastRetrievedAt | ace-reflector.service.ts 改用 lastRetrievedAt |
| B-7 | 🟠 严重 | formatForContext 使用 emoji 替代 [成功]/[失败] | ace-retrieval.service.ts 改用设计文档格式 |
| G-3 | 🟡 一般 | 缺失 fork bomb 命令规则 | risk-engine.service.ts 新增规则 |
| G-4 | 🟡 一般 | curl/wget 匹配过于宽泛 | risk-engine.service.ts 改为精确前缀 |
| G-5 | 🟡 一般 | 检索注入缺少 Token 预算 | ace-retrieval.service.ts 新增 estimateTokens + 裁剪 |

### 修改文件
- ace-reflector.service.ts
- ace-retrieval.service.ts
- risk-engine.service.ts

### 验证
- typecheck ✅ | lint ✅ 0e 20w | test ✅ 91 passed (90b+1f)
